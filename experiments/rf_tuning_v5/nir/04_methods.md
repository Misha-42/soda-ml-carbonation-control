# 04. Методы

## 1) Что сюда писать
- Почему выбраны RandomForest и XGBoost как baseline.
- Кратко описать подготовку признаков (числовые признаки + заполнение пропусков).
- Объяснить стратегию разбиения данных (time-based split или по порядку строк).

## 2) Какие таблицы и графики сюда вставлять
- Таблица с конфигурациями экспериментов (`rf_small`, `rf_medium`, `xgb_small`, `xgb_medium`).
- Схема шагов обучения и оценки.

## 3) Какие данные из проекта будут использоваться
- Конфигурации экспериментов из `src/train_baseline.py`.
- Логика feature preparation из `src/features.py`.
